import { Meteor } from 'meteor/meteor';
import '../imports/api/github.js';
import '../imports/api/userInfo.js';
import '../imports/api/publish.js';

Meteor.startup(() => {
  // code to run on server at startup
});
